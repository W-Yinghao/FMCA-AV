# 服务器确认清单：FMCA-AV Results 定稿

核查基准：`certificate-framework-wave0`，提交 `ae02ffe2f0bb06a60f31539536e762c8e7bbc3a7`（2026-09-15）。本清单不属于论文正文或 supplement。

当前 GitHub 汇总已确认：基线学习率选择与早期秩比较完成；尚在进行的是 ridge β=0 的第 20/200 epoch 复现。旧的最终模型 β=0 对照已经完成，本版 Results 使用的是这些旧的匹配对照，没有把在运行的实验写成已完成。

## 1. 性能表的最佳值：优先完成一次统一导出

请对本版主表的每个单元格导出：数据集、方法、训练目标、优化图像数、训练 epochs、backbone、取特征的层、投影配置、评价器、准确率原始小数、对应模型标识、训练代码版本、评价代码版本、重复编号。

主表统一采用 CIFAR-10、ResNet-18、45,000 图像优化子集、200 epochs、标准化逐层线性评价。它展示指定配置在已完成评价中的逐列最大值，不是均值，也不是一份模型在所有列同时达到的成绩。

需重点核对：

| 单元格 | 本稿使用值 | 对应依据 |
|---|---:|---|
| FMCA-AV final-level multiview，Stage 2/3/4 | 69.04 / 84.93 / 88.90% | 五份 final_mview 的逐层 JSON；已读取 |
| Hierarchical FMCA-AV，Stage 2 | 80.90% | 9 月 14 日里程碑复现；高于原五次评价的 80.69% |
| Hierarchical FMCA-AV，Stage 3/4 | 85.61 / 85.58% | 原五次评价和复现的已报告最大值 |
| BYOL，Stage 4 | 74.76% | `results/baseline_layerwise/byol_bn_seed1.json` 中原值 0.7475999593734741；部分文字汇总误写 74.75 |
| VICReg / Barlow Twins / SimCLR / MoCo v2，Stage 4 | 88.61 / 87.72 / 86.36 / 83.84% | 最新按验证集选择学习率的结果，替代旧的 0.03 结果 |

最终层 SGD 评价的 89.19%（CIFAR-10）和 61.96%（CIFAR-100）已在独立表中保留。不要用 89.19 替换标准化评价的 88.90，却保留基线的标准化分数。

此外，请检索**所有已完成的同预算、同评价器 FMCA-AV 配置**，确认是否还有更高且可比较的结果。更强的不同目标必须另列明确方法行。原 17 页论文中的 CIFAR-10 90.3%、CIFAR-100 70.3%（800 epochs）和 ImageNet-100 61.7%（200 epochs）只见于原论文表，本次未核实其原始记录；若要纳入，提供训练与评价记录、backbone、视图数、目标以及对应基线，并设独立预算表，不能直接拼进当前 200-epoch CIFAR 表。

输出建议：`performance_final.csv` 与 `performance_sources.json`。每个最大值附带该比较组的重复数、均值、标准差和范围，便于表后统计完整披露。

## 2. Ridge β=0：区分已有最终评价和新里程碑复现

请给出新复现的完成数量、最终运行状态，以及每个模型在第 20/200 epoch 的逐层评价。与同批完整 ridge 模型按相同重复编号配对，导出 Stage 1--4 的均值、标准差及成对差值；确认唯一损失差异为 β=128→0。

本稿主消融表暂用**已有三组匹配的完成模型**：CIFAR-10 Stage 2/3/4 完整模型 78.60/85.44/85.46，对照 80.00/83.94/83.53；CIFAR-100 完整模型 54.97/58.73/56.06，对照 53.70/58.30/55.07。不要用新的完整模型与旧的 β=0 直接组成“配对增益”。

本稿不包含第 20 epoch 的因果或收敛速度结论。新结果完成后替换整组匹配表，而不是只挑其中更好的单元格。

## 3. Gram 表：确认已经采用正确字段和同一测量协议

本稿没有使用旧文中 0.160/0.167 等顶层 surrogate 值作为精确坐标误差。使用的是以下文件 `curve["20000"]["gram"]["projection"]`：

`results/validity/bigcal_{c10,c100}_convergence_{product_endpoint,additive_mview,final_mview}.json`

已读出的三元组为（绝对误差、端点范数、相对误差）：

| 数据集 | 分层 | 仅相邻目标 | 仅最终层多视图 |
|---|---|---|---|
| CIFAR-10 | 1.489282, 9.249549, 0.161011 | 1.515900, 9.125442, 0.166118 | 1.940832, 4.019990, 0.482795 |
| CIFAR-100 | 1.575668, 9.386764, 0.167861 | 1.571517, 9.223890, 0.170375 | 2.130958, 4.396498, 0.484694 |

这些均为单个固定模型的点估计，全部状态保留 128 维；不是三次实验的均值。本稿已经明确这一点。

请补齐每份 JSON 的模型指纹对应关系、训练配置/版本、20,000 校准图像的身份清单、10,000 评价图像身份、实际样本计数、归一化所用特征池、投影头是否训练、阈值与所有状态保留维数。当前代码显示使用 30,000 图像优化训练的模型，并合并校准/验证集合进行坐标估计，评价使用独立 test 集；请以运行时配置和图像身份确认。

若服务器已有**当前 45,000 图像完整 ridge 与其 β=0 对照的 Gram-corrected 分析**，请优先导出。这样可以使性能消融与算子消融对应同一批模型；目前的“仅相邻目标”一行不是 β=0 对照。此项需要的是结果确认或冻结后评价，不是必然重训。

## 4. 确认验证数据、学习率选择与模型选择的实际边界

现有 ridge 代码在验证调用中更新端点平均值；本稿在实验细节用一句话说明了这一事实，并将 45,000 称为优化图像子集，而没有声称模型只接触过这些图像。请确认这批结果的实际调用行为是否一致，以及新增复现是否改变过该行为。

请核实基线 5,000 图像验证集是否在 probe 拟合时被排除、是否与测试集完全分开、四个方法采用的选择分数是否确为 Stage 2--4 平均准确率。不要仅根据“holdout”字段推断所有图像身份独立。

性能表的逐列最大值是描述性最佳完成结果。若正式投稿希望使用“验证集选择的单个模型在测试集上的分数”，请提供相应选择记录，这与当前最大值表是不同统计口径。

## 5. 统一重复数量、层名和评价器字段

请导出全部表的评价器版本、特征标准化方式、分类器拟合图像数和超参数，确认五次主结果均值、三次匹配消融和新增复现的归属。

部分旧 `layerwise_profile.json` 的 `level_role` 把层位置写错，但 `backbone_layer` 与实际配置是可识别的。本稿统一使用 `layer1`--`layer4` 对应 Stage 1--4，并按配置 `[1,2,3]` 将 chain 读出对应 Stage 2--4。请检查原绘图与 CSV 是否也采用这一对应。

## 6. 截断结果只按数学实现归类

已确认旧的 CIFAR-10 full/β=0 使用“链截断＋辅助 ridge 分数”，较新的 α=0、T=2、T=4、K=256、parallel 等实验使用“链截断＋辅助截断分数”。本稿没有把这些混合版本表作为单因素消融。

请为需要保留的截断结果导出两处归一化方式、辅助 Gram 是否参与求导、矩阵目标构造、训练代码版本及运行开始/恢复信息。对仅有五个 epoch 的诊断，不把其准确率作为正式性能数值；仅使用保留维数的观察。

## 7. 可选扩展：补齐跨数据集的外部性能比较

当前更新后的、评价协议已核对的七个外部基线主表只有 CIFAR-10。本稿报告 CIFAR-100 的 FMCA-AV 性能、逐层分析和消融，但没有填造外部基线结果。若服务器已有 CIFAR-100、Tiny ImageNet、ImageNet-100 的完整可比基线，请按第 1 项同样的元数据格式提供，单独组织数据集和训练预算。

---

建议一次返回四个文件：`performance_final.csv`、`ridge_beta0_matched.csv`、`gram_analysis_final.csv`、`experiment_manifest.json`。论文所需数值与上述确认状态分开存放；工程标识只出现在 manifest 中。
