# 理论补全与边界说明（2026-08-24）

## 本轮已经完成

论文主文件 `main.tex` 已补入可编译的完整证明，覆盖：

1. Markov 条件期望算子的方向、压缩性与 Chapman–Kolmogorov 分解；
2. FMCA 白化交叉协方差目标的变分上界与等号条件；
3. 固定父样本数时，多条件后代平均的精确方差分解；
4. 正交特征子空间下的投影缺陷 telescoping 展开；
5. 一般非正交坐标下的 Gram 修正定理；
6. 奇异值下界、谱隙条件下的方向稳定性；
7. 正交 gauge 不变性、条件期望压缩性与路径质量单调性；
8. Horn 型乘法奇异值不等式，以及逐模态乘积可高估或低估的反例；
9. 有限样本乘积误差传播和总体证书下界；
10. 冻结、有界坐标下的矩阵 Bernstein 显式半径。

同时补入 Wedin（奇异子空间扰动）、Tropp（矩阵 Bernstein）以及 HFMCA 的准确参考文献。全文经过 `pdflatex → bibtex → pdflatex × 2` 编译，无 LaTeX 错误、无未定义引用；现存两个 BibTeX 警告来自旧文献条目的空作者字段，与本轮理论无关。

## 最重要的新定理：一般坐标并不等于正交投影

设第 `l` 层的有限维特征函数由合成映射 `F_l` 表示，Gram 矩阵为

\[
G_l=F_l^*F_l.
\]

只有 `G_l=I` 时，`F_l F_l^*` 才是正交投影。一般情况下，正交化后的映射和真正的投影是

\[
Q_l=F_lG_l^{-1/2},\qquad
P_l=F_lG_l^{-1}F_l^*.
\]

若原始相邻交叉矩阵是

\[
B_{l,l+1}=F_l^*T_{l,l+1}F_{l+1},
\]

那么在正交坐标中，正确的路径组合为

\[
G_0^{-1/2}B_{0,1}G_1^{-1}B_{1,2}\cdots
G_{L-1}^{-1}B_{L-1,L}G_L^{-1/2}.
\]

直接相乘 `B_{0,1}B_{1,2}...` 插入的是 `F_lF_l^*`，不是正交投影。其缺陷可以精确拆为

\[
I-F_lF_l^*=(I-P_l)+Q_l(I-G_l)Q_l^*.
\]

第一项才是有限子空间遗漏；第二项是坐标度量或 ridge 收缩造成的误差。

## 与当前评估和训练实现的关系

当前冻结坐标使用

\[
W_l=(\widehat R_l+\rho s_lI)^{-1/2}.
\]

即使忽略有限样本误差，其总体 Gram 仍为

\[
G_l=R_l(R_l+\rho s_lI)^{-1}\ne I.
\]

因此，现有真实图像评估器和受控链的经验评估器直接相乘 ridge 白化后的交叉矩阵时，计算的都是“带正则化接口收缩的缺陷”，不能原样解释成论文定理中的“正交投影缺陷”。总体解析矩阵的恒等式不受这一问题影响，但有限样本受控数值也需要 Gram 修正后重算。

同一个不匹配也出现在训练损失中：当前训练在 ridge 批白化后直接组合边矩阵，没有插入内部 Gram 逆。所以已有准确率、逐层线性探针和有效秩可以作为“旧 ridge 度量下的稳定化替代目标”产生的经验现象，但它们不证明已经训练了论文定义的纯投影 closure objective。这与随机未训练网络获得更低归一化缺陷、层级模型校准不收敛等现象方向一致，但目前只能说是一个有理论根据的候选机制，不能说已经证明了因果关系。

下一次服务器修改有两种数学上自洽的选择：

- 保持“正交投影缺陷”的论文对象：同时修正评估器和训练损失，保存每层 Gram，按上式插入 Gram 逆，或在预先确定的有效秩子空间内重新正交化；然后重跑受控经验证书并重训最小关键图像组。
- 保留现有训练乘积：把训练对象明确改称“正则化接口替代目标”，使用一般接口定理描述它，不再给它纯子空间语义；评估时仍用 Gram 修正量，并明确降低训练与投影定理等同的主张。

按照现有论文叙事，第一种更合适。修正前，真实图像的绝对缺陷、归一化缺陷和跨模型缺陷比较都应保持暂停；受控链的经验 closure 数值也要修正后复核。准确率、线性探针和有效秩仍然是可报告的旧 surrogate 训练现象，但不能被写成理论中纯投影训练目标的结果。只有受控链的直接总体解析计算不受这一实现问题影响。

## 有限样本证书已经补到哪一步

设真实端点矩阵为 `D`，真实路径乘积为 `P`，各相邻估计误差半径为 `r_l`，端点半径为 `r_D`。若真实相邻算子都是压缩映射，则乘积误差满足

\[
r_P=\prod_l(1+r_l)-1.
\]

令经验缺陷为

\[
\widehat d=\|\widehat D-\widehat P\|_2.
\]

论文现在区分两个不同结论：

1. 真实端点第 `k` 个奇异值的下界：

\[
[\sigma_k(\widehat P)-\widehat d-r_D]_+
\le \sigma_k(D).
\]

2. 真实总体“路径证书”的下界：

\[
[\sigma_k(\widehat P)-\widehat d-r_D-2r_P]_+
\le [\sigma_k(P)-\|D-P\|_2]_+.
\]

第二个结论更保守，但它才对应总体路径证书。旧受控经验中 13%–21% 的小幅总体越界，说明原经验公式至少缺少有限样本半径。但因为旧经验乘积同时含有未修正的 ridge bias，新定理不会追溯性地使那些旧数字成为有效的纯投影证书。

矩阵 Bernstein 推论现在能在“网络、坐标和 Gram 修正均已冻结”的前提下，为每个端点或相邻矩阵给出显式高概率半径，并通过 union bound 组成联合事件。它不要求不同矩阵估计彼此独立。

## 尚未完成、也不应伪装成已完成的部分

1. 还没有把样本均值、样本协方差、有效秩选择、Gram 逆和逆平方根的误差一起传播成一个无条件定理；这需要最小特征值或截断阈值假设。
2. 还没有在服务器的评估器和训练损失中实现 Gram 修正，也没有重算受控经验证书或重训关键图像组。
3. 无谱隙时，Weyl 下界只认证排序后的奇异值大小，不能认证“同一个具体语义方向”。
4. 单侧旋转可能增大、减小或保持缺陷，不能再作为“必须恶化”的验收条件。
5. telescoping 各项可能相互抵消，也依赖展开方向；它们是接口泄漏诊断，不是唯一的、因果性的层归因。
6. 多后代方差定理针对固定独立父样本数和总体中心化的原始交叉矩；固定总视图预算、估计白化或训练梯度不享有无条件单调下降保证。

## 当前理论结论的最稳妥表述

在真正的 Markov 视图链和共同正交特征坐标下，总体条件期望算子严格分解；有限维路径与直接端点之间的差异可精确写成各内部接口的投影遗漏经过上下游传播后的和。对一般坐标，必须插入 Gram 逆，或者承认测量中同时包含坐标度量误差。在给定联合算子估计半径后，端点谱下界和总体路径证书都有严格的有限样本修正项。
