# Gate 1 附录 7：v7 探索臂（下一轮设计，先行进队）

**日期：** 2026-08-18　**性质：** 单种子探索臂（exploratory，非判定行）；任何胜出臂升 3 seeds 后才可进入报告表。主判定行仍是 v6d β=128 × 3 seeds（在跑）。

## 四条臂（相对 v6d-b128 基线各改一件事）

| 臂 | 改动 | 假设 |
|---|---|---|
| A `v7x_anneal` | α: 0.2 →(cosine)→ 0 | 引导期后释放 bootstrap，收敛目标回到冻结代数的 α→0 正典形态；预期缺陷更低、probe 持平或更好 |
| B `v6d_b256`（已建配置） | β 128→256 | 闭合权重上行空间（2-ep probe：缺陷 0.446 vs 0.467） |
| C `v7x_stopgrad` | closure 内 sg(C^dir) | 预注册消融：复合单向追赶被测端点（可证伪测量语义更纯），端点项不被闭合拖拽 |
| D `v7x_m8` | endpoint 视图 4→8 | Prop 3 方差降低强化 Ĉ^dir；与 flat M=8（85.3%）同视图数可比 |

## 选择判据（200 epochs 后）

probe 与 held-out 缺陷的 Pareto：任何臂若 probe ≥ b128-3seed 均值 + 0.5pt 或缺陷 ≤ 0.7×，升 3 seeds；全负则 b128 为 v7 最终配置。

## 纪律

单种子臂不进主表、不做 claim；α 调度与 stop-grad 均在冻结代数的消融清单内（handoff §5）。
