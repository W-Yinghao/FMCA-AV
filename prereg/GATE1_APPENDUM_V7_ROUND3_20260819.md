# Gate 1 附录 8：v7 第三轮臂（组合 + 结构 + 时长）

**日期：** 2026-08-19　**性质：** 单种子探索臂；胜者升 3 seeds 后方可进报告表。基线 = v6d-b128（72.4%，缺陷 0.357）。

## 第二轮臂的中期信号（设计依据）

anneal：完成，缺陷 0.296 / probe 71.7（边际）；m8：ep75 时 train closure **0.155**（基线同期 ~0.35，Prop 3 方差降低使复合目标更"实"）；stopgrad：dir_trace **98.3**（闭合不拖端点）；b256：与 b128 轨迹重合，无增益。

## 第三轮五臂（相对 b128 的改动）

| 臂 | 改动 | 假设 |
|---|---|---|
| `v7e_leaf` | + 末层 flat 项（f_head(mean endpoint views)↔views，权重 1） | 结构缺口假设：v7 端点奖励在 layer2↔leaf，flat 的 84% 引擎在纯末层；"flat 目标+闭合正则"形态直接补末层容量 |
| `v7e_m8_stopgrad` | M=8 + sg(C^dir) | 组合两个正交增益：更实的复合目标 × 端点不受拖累 |
| `v7e_m8_leaf` | M=8 + 末层项 | 闭合增益 × 末层容量 |
| `v7e_m8_stopgrad_leaf` | 全组合 | 上两者叠加 |
| `v7e_best400` | M=8 @ 400 epochs | b128 在 ep183 各项仍在改善——时长约束探顶 |

## 附带分析任务（非训练）

Stage-C 测量地板标定：全部行（含 flat）的 held-out 缺陷 ≥0.31，疑为 ridge 1e-3 / K=128 / cal 2500 的估计噪声地板——用已存 checkpoint 在测量 ridge ∈ {1e-2, 1e-1} 下重测（post-hoc 披露，排下一批）。

## 判据（同附录 7）

probe ≥ 72.9（基线均值+0.5）或缺陷 ≤ 0.25（0.7×基线）；胜者升 3 seeds。单种子不进主表。
