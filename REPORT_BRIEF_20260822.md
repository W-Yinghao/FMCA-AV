# 汇报材料速览（2026-08-22）

图表在 `outputs/report_20260822/`，重跑 `scripts/make_report_figures.py`
可用最新结果刷新（CPU，秒级）。以下按建议的叙事顺序。

## 1. 框架是什么（fig 无需）

路径支持的依赖证书三元组 (C^dir, C^comp, Δ^CK)：在深度对齐的
嵌套随机视图层级上，比较"端点直测算子"与"逐边条件算子的有序
全矩阵复合"，闭合亏差 Δ^CK 经 Weyl 界给出证书谱 s^cert。
三条红线：必须中心化、每层共享坐标系、全矩阵复合（禁 σ-乘积）。
Wave 0：六个冻结反例 + Gaussian-Hermite 解析链全部按预期行为。

## 2. 训练故事（fig_arc, fig_v8_main）

- V7 全方法优化弧：48.3 → 72.4（β=128 重标定）→ 75.4（M=8+叶项）
  → 82.8（EMA 算子目标 = 关键突破）→ 85.4（全保真树），每步单变量。
- v8 终门（关键行已升 5 种子，零崩塌）：flat 锚 89.0±0.1 /
  flat-2view 85.3；V7 85.0±0.7 —— 与 flat-2view 持平，E1 九项两两
  全胜其余层级变体；additive 78.3±0.3；product_only 57.0±6
  （不稳定，即反例 4 的实证版）。
- E2（±1pt 非劣性 vs 89.0 锚）未过：层级树的代价约 2-3pt +
  谐波残差约 1.6-1.9pt，已在 flat 归因阶梯中定量分解。

## 3. 仪器故事——本周的核心进展（fig_instrument）

- 校准 2500→5000（合池 val，等同 held-out）：所有 defect 统一下降
  ~0.04 —— Thm 1 的 ε_n 噪声地板被实证并量化。
- 但显式（V7, 0.209）与隐式（additive, 0.199）闭合在任何 ridge 下
  不分离：E1 的薄边缘打平是真实的，非样本量伪影。
- 诚实结论：本仪器分辨率下，加性训练达到与显式复合训练相当的
  测量闭合。框架的实证价值 = 仪器本身（下面三条），而非
  "显式训练独有的 defect 优势"。
- flat 行合池已落盘：**仪器在 5000 校准下清晰分离"为闭合训练过"
  （V7/additive ≈0.20）与"没训练过"（flat ≈0.40-0.43）**——正面
  灵敏度证据。product_only 在合池下随 ridge 非单调（0.24→0.61），
  与其训练不稳定一致。

## 4. 仪器交付物三支柱（fig_spectroscopy, fig_depth, fig_plugin）

1. **通道谱学**：扫某一边的增强参数，只有该边的算子范数移动
   （edge1 灰度扫描时 edge0 恒定）——边定位归因成立，并已在 v8
   冠军 checkpoint 上复现：min_scale 扫描只动 edge1（10.6→11.5
   单调，edge0 钉在 4.16）；flip 扫描零响应——一条"空通道"，
   backbone 对翻转不变（CIFAR 类别镜像对称），仪器如实报告。
2. **探测深度画像**：stage 级保留率区分预训练（深层保持 ~0.9）
   与随机初始化（单调衰减到 0.66）；扩展已齐：resnet152 末段保留
   0.884、resnet50 0.892、resnet18 降至 0.792 —— 越深的预训练网
   深层保留越多，与逐层探测精度（best 均为 stage 4）一致。
3. **Plug-in 研究**：闭合正则挂到三个外部 SSL 基座，defect 全部
   下降（Barlow 0.54→0.29，VICReg 0.56→0.30，FroSSL 0.44→0.19）；
   精度交换率方法相关（Barlow −1.9pt / VICReg −10.6pt /
   FroSSL +6.7pt 但其基座在统一配方下不稳定，已排除出主分析）。
   β∈{16,48} 扫描在跑（β=32 为默认）。

## 5. 负控、消融与 CIFAR-100（过夜落盘）

- **CIFAR-100 pilot 9/9 齐**：flat 61.5±0.4 / V7 55.9±0.4 /
  additive 48.5±0.5。**亮点：defect 排序 V7 (0.372-0.378) <
  additive (0.389-0.409) < flat (0.480-0.496)，种子间零重叠**——
  第一个显式闭合训练在仪器上与加性分离的设定（CIFAR-10 上打平），
  且 V7 精度同时高出 additive 7.4pt。
- α=0 消融：83.4%（vs V7 均值 85.0）——EMA 目标独立基本治愈组合
  冷启动（历史 α=0 崩到 48），α bootstrap 仍贡献约 1.6pt。
- M=4 消融：82.1% —— M=8→4 损失约 2.9pt，端点视图数重要。
- **parallel star 树负控（CIFAR-10）：零结果**。打破 Markov 嵌套后
  probe 84.1%、defect 0.434，对比 nested V7 的 85.0±0.7 / 0.370±0.050
  —— z=1.28，落在种子散布内。已核实配置确实生效（hparams 记录
  `mode: parallel`），非无操作。**诚实表述：在 CIFAR-10 的仪器
  分辨率下，星型构造与嵌套构造无法区分**；已补两个种子，并把该
  负控搬到 CIFAR-100（仪器在那里有分辨力）重做。这与第 3 节的
  主线一致：CIFAR-10 上仪器分辨率不足，CIFAR-100 上才见结构信号。

## 6. 一句话定位

“多层多视图 SSL 的显式算子复合 + 端点一致性表述，配一台
可证伪的闭合测量仪器”——训练上与 flat-2view 持平且远超其它
层级基线，仪器上给出谱学归因、深度画像与跨方法 plug-in 改善。
